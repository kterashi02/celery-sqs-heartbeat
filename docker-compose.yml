# ローカル SQS 互換サーバ(ElasticMQ)を起動する。
services:
  elasticmq:
    image: softwaremill/elasticmq-native:1.6.11
    ports:
      - "9324:9324"   # SQS API
      - "9325:9325"   # 管理UI (http://localhost:9325)
    volumes:
      - ./elasticmq.conf:/opt/elasticmq.conf
